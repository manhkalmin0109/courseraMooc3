function handleMouseOver(image) {
    // Code to execute when the mouse is over the image
    // For example, you can change the border color or perform other actions
    image.style.border = "2px solid #ff0000";
}

function handleMouseOut(image) {
    // Code to execute when the mouse leaves the image
    // For example, you can reset the border color or perform other actions
    image.style.border = "none";
}
